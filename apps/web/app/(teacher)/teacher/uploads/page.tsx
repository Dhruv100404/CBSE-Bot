import { PlaceholderPage } from "@/components/placeholder-page";

export default function TeacherUploadsPage() {
  return (
    <PlaceholderPage
      title="Teacher uploads"
      description="Uploaded worksheets, question papers, and answer keys will be processed, tagged, and reviewed from this queue."
      badge="Document ingest"
      primaryAction="Upload worksheet"
      secondaryAction="Track processing"
    />
  );
}
