import { PlaceholderPage } from "@/components/placeholder-page";

export default function TeacherQuestionBankPage() {
  return (
    <PlaceholderPage
      title="Question bank"
      description="Search, filters, metadata tags, and replace/regenerate flows will converge in this question browsing surface."
      badge="Question bank"
      primaryAction="Search questions"
      secondaryAction="Review tags"
    />
  );
}
