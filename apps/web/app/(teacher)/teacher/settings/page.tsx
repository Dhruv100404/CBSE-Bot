import { PlaceholderPage } from "@/components/placeholder-page";

export default function TeacherSettingsPage() {
  return (
    <PlaceholderPage
      title="Teacher settings"
      description="Personal preferences, organization defaults, and template preferences will be configured here."
      badge="Teacher settings"
      primaryAction="Update preferences"
      secondaryAction="Manage templates"
    />
  );
}
