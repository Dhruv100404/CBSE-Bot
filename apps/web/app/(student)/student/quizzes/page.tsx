import { PlaceholderPage } from "@/components/placeholder-page";

export default function StudentQuizzesPage() {
  return (
    <PlaceholderPage
      title="Quiz builder and attempts"
      description="This surface will combine quiz mode selection, recent attempts, and blueprint-aware chapter practice."
      badge="Quiz engine"
      primaryAction="Create quiz"
      secondaryAction="Review attempt history"
    />
  );
}
