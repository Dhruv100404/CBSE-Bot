import { PlaceholderPage } from "@/components/placeholder-page";

export default function StudentSettingsPage() {
  return (
    <PlaceholderPage
      title="Student settings"
      description="Profile preferences, language, class level, and notification options will live here."
      badge="Profile settings"
      primaryAction="Update profile"
      secondaryAction="Manage preferences"
    />
  );
}
